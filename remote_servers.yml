---
 - hosts: all
   tasks:
     - name: Create file
       file:
         path: /tmp/file.txt
         state: touch
       when: "'SFTP Servers'" in group_names"
       
     - name: Fetch a file
       fetch: 
         src: /tmp/file.txt 
         dest: /tmp/file.txt 
         flat: yes
       when: "'SFTP Servers'" in group_names"

     - name: Copy the file to the prod server
       copy: 
         src: /tmp/file.txt 
         dest: /tmp/file.txt
       when: "'Production Servers'" in group_names"
