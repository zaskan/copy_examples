- name: Copy a file between remote hosts
  gather_facts: no
  hosts: Production
  tasks:
  
    - name: Get info
      shell: export
      environment:
        ansible_user: "{{ sftp_user }}"
        ansible_password: "{{ sftp_pass }}"
      register: whoami
  
    - debug: msg="{{ whoami }}"
        
